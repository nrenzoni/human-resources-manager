<UserControl x:Class="PLWPF.Employee_UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PLWPF"
             mc:Ignorable="d" >


    <Grid>
        <Grid.Resources>
            <Style TargetType="TextBox">
                <Setter Property="Width" Value="200"/>
                <Setter Property="Grid.Column" Value="1"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="Margin" Value="0,5"/>
            </Style>
        </Grid.Resources>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Label Content="_ID:" Target="{Binding ElementName=txtID}" Grid.Column="0" />
        <TextBox Name="txtID" />

        <Label Content="_First Name:" Target="{Binding ElementName=txtFirstName}" Grid.Row="1"/>
        <TextBox Name="txtFirstName" Grid.Row="1" />

        <Label Content="_Last Name:" Target="{Binding ElementName=txtLastName}" Grid.Row="2"/>
        <TextBox Name="txtLastName" Grid.Row="2"/>

        <Label Content="_Gender:" Target="{Binding ElementName=IsMale}" Grid.Row="3"/>
        <WrapPanel Grid.Row="3" Grid.Column="1">
            <RadioButton Content="Male" IsChecked="True"/>
            <RadioButton Content="Female" Margin="5,0"/>
        </WrapPanel>

        <Label Content="_Birthday" Target="{Binding ElementName=Birthday}" Grid.Row="4"/>
        <DatePicker Name="txtBirthday" Grid.Row="4" Grid.Column="1" Margin="0,5"/>

        <Label Content="_Phone Number:" Target="{Binding ElementName=txtPhoneNumber}" Grid.Row="5"/>
        <TextBox Name="txtPhoneNumber" Grid.Row="5"/>
        
        <Label Content="_Civic Address:" Grid.Row="6"/>
        <StackPanel Grid.Column="1" Grid.Row="6" >            
                <Label Content="Address:"/>
                <TextBox Name="txtEmplyeeAdd"/>
            <WrapPanel>
                <Label Content="Ci_ty:" Target="{Binding ElementName=ComEmplyeeCity}"/>
                <ComboBox Name="ComEmplyeeCity" Width="100" Margin="15,0"/>
            </WrapPanel>      
        </StackPanel>

        <Label Content="_Years of Experience:" Target="{Binding ElementName=txtExperiance}" Grid.Row="7"/>
        <TextBox Name="txtExperiance" Grid.Row="7"/>

        <Label Content="_Recommendations:" Target="{Binding ElementName=txtRecommendations}" Grid.Row="8"/>
        <TextBox Name="txtRecommendations" Grid.Row="8"/>

        <Label Content="_Email:" Target="{Binding ElementName=txtEmail}" Grid.Row="9"/>
        <TextBox Name="txtEmail" Grid.Row="9"/>

    </Grid>
</UserControl>
